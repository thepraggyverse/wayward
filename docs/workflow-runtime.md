# Workflow Runtime

Workflows are phase graphs. A phase may fan out to workers, reduce results, verify evidence, synthesize a report, or request a persisted approval gate. Phase inputs and outputs can declare schemas so downstream phases consume validated objects rather than freeform text.
